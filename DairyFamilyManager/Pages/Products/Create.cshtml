@page
@using DairyFamilyManager.Models
@model DairyFamilyManager.Pages.Products.CreateModel
@{
    ViewData["Title"] = "Add Product";
}

<div class="dfm-pagehead mb-3">
    <div>
        <h1 class="dfm-h1 mb-1">Add Product</h1>
        <div class="dfm-muted">Define product names, base price, and profit rules.</div>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(Model.Error))
{
    <div class="alert alert-danger d-flex align-items-start gap-2" role="alert">
        <i class="bi bi-exclamation-triangle"></i>
        <div>@Model.Error</div>
    </div>
}

<div class="dfm-card p-3">
    <form method="post" class="dfm-form">
        <div class="row g-3">
            <div class="col-12 col-md-6">
                <label class="form-label">Name (EN)</label>
                <input class="form-control" name="NameEn" value="@Model.NameEn" />
            </div>

            <div class="col-12 col-md-6">
                <label class="form-label">Name (AR)</label>
                <input class="form-control" name="NameAr" value="@Model.NameAr" />
            </div>

            <div class="col-12 col-md-6">
                <label class="form-label">Label (EN) (optional)</label>
                <input class="form-control" name="LabelEn" value="@Model.LabelEn" />
            </div>

            <div class="col-12 col-md-6">
                <label class="form-label">Label (AR) (optional)</label>
                <input class="form-control" name="LabelAr" value="@Model.LabelAr" />
            </div>

            <div class="col-12 col-md-4">
                <label class="form-label">Base Price</label>
                <input class="form-control" name="BasePrice" value="@Model.BasePrice" />
            </div>

            <div class="col-12"><hr class="my-2" /></div>

            <div class="col-12 col-md-4">
                <label class="form-label">Factory Profit Type</label>
                <select class="form-select" name="FactoryProfitType">
                    <option value="Fixed" selected="@(Model.FactoryProfitType == ProfitType.Fixed)">Fixed</option>
                    <option value="Percent" selected="@(Model.FactoryProfitType == ProfitType.Percent)">Percent</option>
                </select>
            </div>

            <div class="col-12 col-md-4">
                <label class="form-label">Factory Profit Value</label>
                <input class="form-control" name="FactoryProfitValue" value="@Model.FactoryProfitValue" />
            </div>

            <div class="col-12"><hr class="my-2" /></div>

            <div class="col-12 col-md-4">
                <label class="form-label">Distributor Profit Type</label>
                <select class="form-select" name="DistributorProfitType">
                    <option value="Fixed" selected="@(Model.DistributorProfitType == ProfitType.Fixed)">Fixed</option>
                    <option value="Percent" selected="@(Model.DistributorProfitType == ProfitType.Percent)">Percent</option>
                </select>
            </div>

            <div class="col-12 col-md-4">
                <label class="form-label">Distributor Profit Value</label>
                <input class="form-control" name="DistributorProfitValue" value="@Model.DistributorProfitValue" />
            </div>

            <div class="col-12">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" name="IsActive" checked="@(Model.IsActive)" id="IsActive" />
                    <label class="form-check-label" for="IsActive">Active</label>
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-end gap-2 mt-4">
            <a class="btn btn-outline-secondary" href="/Products"><i class="bi bi-x"></i><span>Cancel</span></a>
            <button class="btn btn-primary" type="submit"><i class="bi bi-check2"></i><span>Save</span></button>
        </div>
    </form>
</div>
